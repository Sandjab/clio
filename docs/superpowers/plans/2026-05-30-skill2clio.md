# skill2clio Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a Claude Code skill `/skill2clio` that converts a skill directory into a `.clio` source using the current CC session as the model (no API key), with an unbounded `clio check` compile-correct loop.

**Architecture:** A single `SKILL.md` under `.claude/skills/skill2clio/`. It drives a 6-step agentic workflow (gather → load-rules → draft → check → loop → report). Durable knowledge (grammar, mapping, annotations, language policy) is read at runtime from `clio/prompts/skill_to_clio_system.md` (single source of truth); the SKILL.md owns orchestration and explicitly overrides that prompt's API-shaped output framing.

**Tech Stack:** Markdown (skill format), `python -m clio check` (in-loop validator). No Python code, no compiler/CLI changes — the spec scopes this to "a file under `.claude/skills/` and nothing else."

**Spec:** `docs/superpowers/specs/2026-05-30-skill2clio-design.md`

> **Note on "tests":** A CC skill is instructions for a session, not Python — it is NOT pytest-testable. The verification model here is **dogfooding**: an executing agent follows SKILL.md on a real fixture and confirms `python -m clio check` passes. Tasks 2–4 are these manual/agentic verifications. The flagship (Task 3, math-olympiad) is the exact skill the API path failed on — passing it proves the loop's value.

---

### Task 1: Author the skill file

**Files:**
- Create: `.claude/skills/skill2clio/SKILL.md`

- [ ] **Step 1: Confirm the prerequisites the skill references actually exist**

Run:
```bash
test -f clio/prompts/skill_to_clio_system.md && echo "prompt OK"
python -m clio check examples/mvp.clio >/dev/null 2>&1 && echo "check subcommand OK"
git check-ignore .claude/skills/skill2clio/SKILL.md && echo "IGNORED (problem)" || echo "trackable OK"
```
Expected: `prompt OK`, `check subcommand OK`, `trackable OK`.

- [ ] **Step 2: Write `.claude/skills/skill2clio/SKILL.md`**

Create the file with EXACTLY this content:

````markdown
---
name: skill2clio
description: Convertit un répertoire de skill Claude Code en source .clio en utilisant la session Claude Code courante comme modèle — aucune clé API, contrairement à `clio import` (chemin LLM) qui exige ANTHROPIC_API_KEY. Boucle agentique gather → draft → `clio check` → fix, sans plafond de retry. USER-INVOCABLE ONLY. DÉCLENCHEURS OBLIGATOIRES : '/skill2clio', 'skill2clio', 'convertis ce skill en clio', 'importe ce skill en .clio', 'récupère ce skill en clio sans clé', 'reconstruis le .clio de ce skill', 'skill vers clio sans clé API'. Ne PAS déclencher sur : compilation .clio → cible (c'est `clio compile`), récupération verbatim d'un skill émis par clio avec sidecar (c'est `clio import --mode strict`), ou paraphrases vagues.
---

# skill2clio — reasoned skill → .clio importer (no API key)

Convert a Claude Code skill directory into a `.clio` source by reasoning, using
THIS session as the model. No Anthropic API key, no `clio[gen]` install. Unlike the
single-shot API path in `clio/skill_to_clio.py`, you have an **unbounded compile-correct
loop**: draft, run `python -m clio check`, read the error, fix, repeat.

## Arguments

`/skill2clio <skill-dir> [output.clio]`

- `<skill-dir>` — path to the skill directory (the one containing `SKILL.md`).
- `[output.clio]` — optional. Default: `<basename of skill-dir>.clio` in the current directory.

## Sidecar courtesy (do this first)

If `<skill-dir>/.clio/` exists (a CLIO-emitted sidecar), print exactly ONE line:

> Note: this skill has a `.clio/` sidecar — `clio import --mode strict <skill-dir>` would
> recover the original source verbatim, deterministically, with no key. Proceeding with a
> reasoned conversion instead (as requested).

Then continue with the reasoned conversion below. **Do NOT read the `.clio/` directory** — the
reasoned path must not cheat off the sidecar.

## Workflow

### 1. Gather
If `<skill-dir>` has no `SKILL.md`, stop and report that it does not look like a skill directory.

Read ALL of the skill's text files, recursively — not just a fixed set of subdirectories.
This mirrors `_gather_skill_files`: walk the whole tree and read every readable text file,
including content in `references/`, `examples/`, `evals/`, `templates/`, a top-level
`README.md`, etc. The judgment-step prompt bodies and strategies often live in `references/`,
so missing them produces a poor conversion.

Treat `process_flow.dot` (if present, anywhere in the tree) as authoritative for FLOW structure.

Ignore (exactly as `_gather_skill_files` does): any hidden file or directory (`.clio/`, `.git/`,
`.DS_Store`), the basenames `_validate.py` and `_cache_key.py`, and binary files.

### 2. Load the mapping rules (single source of truth)
Read `clio/prompts/skill_to_clio_system.md`. Apply its:
- grammar reference,
- mapping table (skill file → CLIO construct),
- annotation rules (`# CLIO-import: ...`),
- output language policy (judgment-step prompt bodies stay in the source skill's language).

**Override that prompt's API-shaped framing.** It was written for a single API call and says
"Output ONLY raw .clio source. No prose." and "respond with a single line starting with
`ERROR:`". IGNORE those two instructions here. In this session you WILL write a file, run
`clio check`, narrate what you map, and iterate. Use the prompt for its KNOWLEDGE (grammar,
mapping, annotations, language), not its output protocol.

### 3. Draft
Write the `.clio` to the output path. Follow the few-shot style in the prompt exactly:
- `CONTRACT name` then 2-space-indented fields — NOT `CONTRACT X { SHAPE: object {...} }`.
- single-line `SHAPE: { ... }` (multi-line record types do not parse).
- `MODE: judgment` or `MODE: exact`; `len(...)` (never `length(...)`).
- `MATCH c.field:` with `CASE variant:` (bare enum variants, no quotes).

Annotate every non-obvious origin with `# CLIO-import: ...` and mark lossy fields
(CACHE / VALIDATE / STRATEGIES / RESCUE / ON_FAIL) as `# CLIO-import: TODO` rather than
inventing values.

### 4. Check
Run: `python -m clio check <output.clio>`

### 5. Loop
If `check` reports an error:
- Read the line/col and message.
- Fix the `.clio`. Common pitfalls (most detailed in the prompt): reserved keywords used as
  identifiers, multi-line `SHAPE`, `IF`/`MATCH` on a bare `str`/`int` param (must be a dotted
  CONTRACT field), step-arg type mismatches, `FLOW.GIVES` field-NAME mismatches, `CACHE`/
  `ON_FAIL` on an exact step (judgment-only), `LANG`/`impl` on a judgment step (exact-only).
  Two more the grammar does not forgive: (1) an exact step's inline shell `cmd:` string cannot
  contain `"` or `\` (the lexer has no escape handling) — if the script emits JSON, map it as
  `impl: mode: code` and keep the logic in the shipped script file rather than inlining a
  `cmd:`; (2) a `WHILE` condition referencing a CONTRACT field needs that field produced by a
  step BEFORE the loop — run the producer once before `WHILE`, then again inside it.
- Re-run `check`.

Repeat until `check` passes. **Brake:** if 4 successive fixes do not reach a clean check, STOP.
Save the best-effort `.clio`, report the last error and which construct is fighting the
grammar. Do not loop indefinitely.

### 6. Report
On success, state:
- the output path,
- a one-line inventory: N contracts, N steps (judgment / exact split), the FLOW shape,
- which fields were annotated `# CLIO-import: TODO` (unrecoverable resilience fields).
````

- [ ] **Step 3: Verify the frontmatter parses and the file is well-formed**

Run:
```bash
python -c "import sys,yaml; t=open('.claude/skills/skill2clio/SKILL.md').read(); fm=t.split('---')[1]; d=yaml.safe_load(fm); print('name:', d['name']); print('desc len:', len(d['description']))"
```
Expected: `name: skill2clio` and a `desc len:` in the few-hundreds. (If `yaml` is unavailable, eyeball the frontmatter instead — two `---` fences, `name:` and `description:` keys.)

- [ ] **Step 4: Commit (precise path — never `git add .claude/` wholesale)**

```bash
git add .claude/skills/skill2clio/SKILL.md
git commit -m "feat(skill2clio): add reasoned no-key skill→.clio importer skill"
```

---

### Task 2: Dogfood on a known-good fixture (agent-development)

This fixture is cheap and has an oracle: `testprojects/skill2clio/agent-development/agent-development.clio` is a known-good `.clio` for it.

**Files:**
- Read-only: `testprojects/skill2clio/inskill/agent-development/`
- Output (temp, not committed): `/tmp/skill2clio-verify/agent-development.clio`

- [ ] **Step 1: Follow SKILL.md to convert the fixture**

Acting as the skill would: gather the files under `testprojects/skill2clio/inskill/agent-development/`, read `clio/prompts/skill_to_clio_system.md`, and draft the `.clio` to `/tmp/skill2clio-verify/agent-development.clio`. (Create the temp dir first: `mkdir -p /tmp/skill2clio-verify`.)

- [ ] **Step 2: Run the in-loop validator**

Run: `python -m clio check /tmp/skill2clio-verify/agent-development.clio`
Expected: exit 0, no error. If it errors, apply Step 5 of the workflow (fix → re-check) until clean.

- [ ] **Step 3: Sanity-compare structure to the oracle**

Run:
```bash
grep -cE '^(CONTRACT|STEP|FLOW|EXPOSE FLOW)' /tmp/skill2clio-verify/agent-development.clio
grep -cE '^(CONTRACT|STEP|FLOW|EXPOSE FLOW)' testprojects/skill2clio/agent-development/agent-development.clio
```
Expected: the two counts are in the same ballpark (exact match not required — reasoned conversion, not verbatim). A wildly different count (e.g. 2 vs 12) means the gather or mapping went wrong; investigate before proceeding.

- [ ] **Step 4: Record evidence (no commit)**

Note in your progress: "agent-development → `clio check` PASS, N constructs (oracle has M)." No repo files change in this task.

---

### Task 3: Flagship verification (math-olympiad — the API-path failure case)

`testprojects/skill2clio/math-olympiad/import.stderr` shows the API path failed here with invented grammar. Passing this is the core proof of the skill's value.

**Files:**
- Read-only: `testprojects/skill2clio/inskill/math-olympiad/`
- Output (temp, not committed): `/tmp/skill2clio-verify/math-olympiad.clio`

- [ ] **Step 1: Follow SKILL.md to convert the fixture**

Gather every content file under `testprojects/skill2clio/inskill/math-olympiad/` (SKILL.md, `references/*.md`, `scripts/check_latex.sh`, `scripts/compile_pdf.sh`; there is no `process_flow.dot`, so infer FLOW from SKILL.md narration and annotate it `# CLIO-import: inferred from SKILL.md narration`). Read the system prompt. Draft to `/tmp/skill2clio-verify/math-olympiad.clio`.

- [ ] **Step 2: Run the in-loop validator and iterate**

Run: `python -m clio check /tmp/skill2clio-verify/math-olympiad.clio`
Expected eventually: exit 0. This fixture is complex (MATCH on model_tier, parallel solver fan, WHILE vote loop). Expect several `check` → fix cycles. Apply the brake at 4 stuck attempts: if a specific construct will not compile, STOP and report it rather than spin.

- [ ] **Step 3: Confirm the failure mode is gone**

Run:
```bash
python -m clio check /tmp/skill2clio-verify/math-olympiad.clio && echo "FLAGSHIP PASS — loop beat the API path"
```
Expected: `FLAGSHIP PASS — loop beat the API path`. (Contrast: the API path's output is in `testprojects/skill2clio/math-olympiad/math-olympiad.failed.clio`, which does NOT pass.)

- [ ] **Step 4: Record evidence (no commit)**

Note: "math-olympiad → `clio check` PASS after K fix cycles" (or, if braked: "braked at construct X — last error Y").

---

### Task 4: Language-policy verification (multilingual + hook-development)

**Files:**
- Read-only: `testprojects/skill2clio/inskill/hook-development/`, `tests/fixtures/skills_for_import/handwritten_fr_pipeline/`, `.../handwritten_es_pipeline/`, `.../handwritten_en_pipeline/`
- Output (temp): `/tmp/skill2clio-verify/*.clio`

- [ ] **Step 1: Convert hook-development**

Follow SKILL.md on `testprojects/skill2clio/inskill/hook-development/` → `/tmp/skill2clio-verify/hook-development.clio`.
Run: `python -m clio check /tmp/skill2clio-verify/hook-development.clio` → expect PASS (iterate per Step 5 if needed).

- [ ] **Step 2: Convert the three handwritten language fixtures**

For each of `fr`, `es`, `en`:
Follow SKILL.md on `tests/fixtures/skills_for_import/handwritten_<lang>_pipeline/` → `/tmp/skill2clio-verify/handwritten_<lang>.clio`.
Run: `python -m clio check /tmp/skill2clio-verify/handwritten_<lang>.clio` → expect PASS.

- [ ] **Step 3: Confirm the language policy held**

For each output, eyeball the judgment-step `DESCRIPTION:` / prompt bodies: the FR output keeps French prose, ES keeps Spanish, EN keeps English. The `.clio` keywords and `# CLIO-import:` annotations are always English. A translated body is a FAIL of the language policy — fix the draft and note it.

- [ ] **Step 4: Record evidence (no commit)**

Note: "hook-development PASS; handwritten fr/es/en PASS, language preserved."

---

### Task 5 (OPTIONAL — confirm scope with user first): visibility note

The spec scopes this to "a file under `.claude/skills/` and nothing else" (§8). But the project's
standing convention is to note user-visible changes in `CHANGELOG.md` / `docs/manual/`. A new
no-key import path is arguably user-visible. **This tension is intentional — do not action it
without the user's call.**

- [ ] **Step 1: Ask the user** whether to add a short CHANGELOG entry (e.g. under an Unreleased/tooling
  heading) and/or a one-line mention in the import docs, or keep the skill self-documenting only.

- [ ] **Step 2:** If yes, add the agreed minimal entry and commit it as a separate `docs:` commit.
  If no, skip — the SKILL.md is self-documenting.

---

## Final integration

- [ ] **Open the PR** (project convention: branch + PR + Gemini review; never merge to `main` directly).

```bash
git push -u origin feat/skill2clio
gh pr create --title "feat: /skill2clio — reasoned no-key skill→.clio importer skill" \
  --body "Adds .claude/skills/skill2clio/SKILL.md. Reasoned (CC-as-model) skill→.clio path, no API key, unbounded \`clio check\` loop. Spec: docs/superpowers/specs/2026-05-30-skill2clio-design.md. Verified by dogfooding on testprojects/skill2clio/inskill/* (incl. math-olympiad, the API-path failure case) + multilingual fixtures."
```

- [ ] After CI green, trigger `/gemini review` manually (auto-trigger is unreliable per project memory).
