# Example: render a Cartouche repo dashboard and commit it to assets/.
#
# Drop this in `.github/workflows/dashboard.yml` of any repo where you want
# the Cartouche dashboard. It runs every 6 hours, regenerates the SVG, and
# commits it back if anything changed.
#
# In your README.md, embed the result with:
#
#     <picture>
#       <source media="(prefers-color-scheme: dark)" srcset="assets/dashboard-dark.svg">
#       <img src="assets/dashboard-light.svg" alt="Cartouche dashboard">
#     </picture>

name: Cartouche · repo dashboard

on:
  schedule:
    # Every 6 hours. Adjust to taste.
    - cron: "0 */6 * * *"
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - ".github/workflows/dashboard.yml"

permissions:
  contents: write   # required to commit the regenerated SVG

concurrency:
  group: cartouche-dashboard
  cancel-in-progress: false

jobs:
  render:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd  # v5

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5
        with:
          python-version: "3.12"

      - name: Install Cartouche
        run: pip install cartouche-svg

      - name: Render light + dark variants
        env:
          # Pass repo identity via env (not direct ${{ }} interpolation in
          # `run:`) so that the bash script never embeds untrusted-context
          # values directly. Today `github.repository` is set by GitHub
          # itself, but propagating the safe pattern here means anyone
          # extending this template (e.g. with a PR title or issue body)
          # starts from a non-injectable baseline.
          # GitHub restricted /repos/{owner}/{repo}/stargazers to a repo's
          # admins and collaborators (2026-07-13). The default GITHUB_TOKEN is an
          # app installation token — neither — so it 403s there even on this repo
          # and FIG. 01 (star history) renders empty. A PAT belonging to an admin,
          # stored as the secret CARTOUCHE_PAT, restores the curve; without one the
          # dashboard still renders, minus that figure.
          GITHUB_TOKEN: ${{ secrets.CARTOUCHE_PAT || secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          mkdir -p assets
          cartouche repo "$REPO" \
            --theme blueprint-light \
            --out assets/dashboard-light.svg
          cartouche repo "$REPO" \
            --theme blueprint-dark \
            --out assets/dashboard-dark.svg

      - name: Commit if changed
        run: |
          if [[ -n $(git status --porcelain assets/) ]]; then
            git config user.name "cartouche-bot"
            git config user.email "cartouche-bot@users.noreply.github.com"
            git add assets/
            git commit -m "chore(cartouche): refresh repo dashboard"
            git push
          else
            echo "No dashboard changes."
          fi
