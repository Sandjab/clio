STEP load
  TAKES: file: str
  GIVES: items: List<str>
  MODE:  exact

STEP classify
  TAKES: item: str
  GIVES: label: str
  MODE:  exact

FLOW pipeline
  load(file="in.csv")
    -> FOR EACH item IN items PARALLEL AS labels:
         classify(item=item)
