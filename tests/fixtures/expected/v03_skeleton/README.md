# classify

Generated by CLIO from a `.clio` source. Implements FLOW `classify`.

## Install

```bash
pip install -e .
```

## Run

```bash
python -m classify
```

Or programmatically:

```python
from classify.flow import run
result = run()
```
