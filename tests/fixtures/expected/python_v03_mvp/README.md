# customer_retention

Generated by CLIO from a `.clio` source. Implements FLOW `customer_retention`.

## Install

```bash
pip install -e .
```

## Run

```bash
python -m customer_retention
```

Or programmatically:

```python
from customer_retention.flow import run
result = run()
```
