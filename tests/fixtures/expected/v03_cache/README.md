# retention

Generated by CLIO from a `.clio` source. Implements FLOW `retention`.

## Install

```bash
pip install -e .
```

## Run

```bash
python -m retention
```

Or programmatically:

```python
from retention.flow import run
result = run()
```
