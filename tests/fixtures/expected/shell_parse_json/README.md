# shell_parse_pipe

Generated by CLIO from a `.clio` source. Implements FLOW `shell_parse_pipe`.

## Install

```bash
pip install -e .
```

## Run

```bash
python -m shell_parse_pipe
```

Or programmatically:

```python
from shell_parse_pipe.flow import run
result = run()
```
